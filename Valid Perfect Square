class Solution {
    public boolean isPerfectSquare(int num) {
        
      double f=Math.sqrt(num);
        int check=(int)f;
        if((double)check!=f)
         return false;
        return true;
    }
}
