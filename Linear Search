class Solution {
    public static int search(int n, int k, int[] arr) {
        
         for(int i=0;i <n ; i++)
        {
            if( k == arr[i])
            return i+1;
        }
        return -1;
    }
}
